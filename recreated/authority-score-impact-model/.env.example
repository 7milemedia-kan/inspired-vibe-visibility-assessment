# No external API key is required for URL assessments.
PORT=5000
# Set a password of at least 12 characters to enable the private leads dashboard.
ADMIN_KEY=
# Set a random session secret for deployment; empty generates one at startup.
SESSION_SECRET=
# HUBSPOT_TOKEN alone does not enable CRM sync; that integration is unfinished.
